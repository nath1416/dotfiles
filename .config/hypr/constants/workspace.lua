local musicWorkspace       = "music"
local extraWorspace        = "extra"
local nbWorspacePersistent = 5


local workspace = {
    musicWorkspace       = musicWorkspace,
    extraWorspace        = extraWorspace,
    nbWorspacePersistent = nbWorspacePersistent
}
return workspace
